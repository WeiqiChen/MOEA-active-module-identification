function nonlcon = connectivity_constrain(x)

% global G;
% 
% ind = find(x==1);
% adj = G(ind,ind);
% % nonlcon = algebraic_connectivity(adj);
% if algebraic_connectivity(adj)>0
%     nonlcon = 0;
% else
%     nonlcon = -1;
% end

nonlcon = 0;   % test

